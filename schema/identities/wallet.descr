=short

<p>Collect things do not want to share with other people, for instance
your private keys.</p>

=schema

==xml
<element name="wallet" type="c:Wallet"
    substitutionGroup="b:native" />

<complexType name="Wallet">
  <complexContent>
    <extension base="b:Native" />
  </complexContent>
</complexType>
==end

=description

<p>The wallet <SCHEMA>b:has</SCHEMA> collects the private identities
of some person or organization.  The private identity units should be
stored encrypted inside the wallet.</p>
